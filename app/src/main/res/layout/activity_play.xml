<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_werewolf"
    android:padding="10dp">

    <TextView
        android:id="@+id/turn_description"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        style="@style/DefaultFont"
        android:text="@string/werewolves_turn_description"
        android:textSize="20dp" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/list_players"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="120dp"
        android:layout_marginBottom="80dp" />

    <Button
        android:id="@+id/bt_next_turn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:layout_marginBottom="10dp"
        android:background="@drawable/button"
        android:text="@string/bt_next_turn"
        android:paddingHorizontal="20dp"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</android.support.constraint.ConstraintLayout>